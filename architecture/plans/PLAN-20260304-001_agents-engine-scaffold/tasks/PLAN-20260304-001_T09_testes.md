---
plan_id: PLAN-20260304-001
task_id: T09
title: "Criar suite de testes unitários e de integração"
agent: DOM-04
model: GPT-5.1-Codex
status: PENDENTE
depends_on: [T05, T06, T08]
parallel_with: []
---

## Tarefa T09 — Testes (unit + integration)

**Plano pai:** [PLAN-20260304-001](PLAN-20260304-001_agents-engine-scaffold.md)  
**Agente executor:** DOM-04  
**Modelo sugerido:** `GPT-5.1-Codex` — testes com mock de Protocol + respx + pytest-mock requerem geração de código precisa  
**Depende de:** T05 (providers), T06 (infrastructure), T08 (audit)  
**Paralelo com:** — (ultima tarefa de implementação)

---

### Objetivo

Criar a suite completa de testes do scaffold: fixtures globais em `conftest.py`, testes unitários de todos os componentes e testes de integração da infra GitHub mockada via `respx`.

---

### Cobertura Obrigatória (mínimo 80% global; domain: 100%)

| Arquivo de teste | O que cobrir |
|---|---|
| `conftest.py` | Fixtures: `mock_settings`, `mock_github_port`, `mock_audit_port` |
| `test_exceptions.py` | Hierarquia de herança; `GitHubAPIError` com `status_code`; `isinstance` checks |
| `test_decision_record.py` | Campos obrigatórios; Literal enforcement; `frozen=True`; `confidence_score` bounds; `rn_triggered` tuple |
| `test_audit_ledger.py` | `emit()` chama `publisher.emit()`; serialização JSON correta; prefixo obrigatório |
| `test_provider_factory.py` | Seleção por `ai_provider`; fallback order; `LLMProviderError` em provider desconhecido |
| `test_github_client.py` (integration) | `respx` intercepta `httpx`; retry em 429; retry em 500; sucesso em 200; `GitHubAPIError` após retries |
| `test_comment_publisher.py` (integration) | `publish()` posta comentário correto; `emit()` serializa `DecisionRecord` |

---

### Especificação Técnica

#### `tests/conftest.py`

```python
import pytest
from unittest.mock import MagicMock
from deep_ion.agents_engine.settings import Settings
from deep_ion.agents_engine.domain.protocols import GitHubPort, AuditPort

@pytest.fixture
def mock_settings() -> Settings:
    return Settings(
        github_token="fake-token",
        github_repo="org/repo",
        ai_provider="deterministic",
    )

@pytest.fixture
def mock_github_port() -> GitHubPort:
    return MagicMock(spec=GitHubPort)

@pytest.fixture
def mock_audit_port() -> AuditPort:
    return MagicMock(spec=AuditPort)
```

#### Padrão dos testes unitários

```python
import pytest
from pydantic import ValidationError
from deep_ion.agents_engine.audit.decision_record import DecisionRecord

@pytest.mark.unit
class TestDecisionRecord:
    def test_valid_record(self) -> None: ...
    def test_frozen_immutability(self) -> None:  # lança ValidationError/AttributeError
    def test_invalid_literal_decision(self) -> None:  # lança ValidationError
    def test_confidence_score_out_of_range(self) -> None:  # < 0.0 ou > 1.0
    def test_rn_triggered_is_tuple(self) -> None:
```

#### Padrão dos testes de integração

```python
import pytest
import respx
import httpx
from deep_ion.agents_engine.infrastructure.github_client import GitHubClient

@pytest.mark.integration
class TestGitHubClient:
    @respx.mock
    def test_get_success(self, mock_settings) -> None: ...

    @respx.mock
    def test_retry_on_429(self, mock_settings) -> None:
        # Simula 2x 429 + 1x 200; verifica 3 chamadas totais

    @respx.mock
    def test_raises_after_exhausted_retries(self, mock_settings) -> None:
        # Simula sempre 500; verifica GitHubAPIError lançada
```

---

### Artefatos de Saída

- `agents-engine/tests/conftest.py` — fixtures globais
- `agents-engine/tests/unit/agents_engine/test_exceptions.py`
- `agents-engine/tests/unit/agents_engine/test_decision_record.py`
- `agents-engine/tests/unit/agents_engine/test_audit_ledger.py`
- `agents-engine/tests/unit/agents_engine/test_provider_factory.py`
- `agents-engine/tests/integration/agents_engine/test_github_client.py`
- `agents-engine/tests/integration/agents_engine/test_comment_publisher.py`

---

### Critérios de Aceite

- `uv run pytest -m 'not e2e'` passa com 100% dos testes (executado de dentro de `agents-engine/`)
- `--cov-fail-under=80` não bloqueia o CI
- Cobertura de `domain/` = 100% de branches
- Nenhum teste usa `os.environ` diretamente — apenas fixtures
