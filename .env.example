# =============================================================================
# deep-ion-agents — Variáveis de ambiente
# Copie este arquivo para .env e preencha os valores reais.
# NUNCA faça commit do .env com valores reais.
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub (obrigatórias)
# Em GitHub Actions estes valores são injetados automaticamente pelo runner.
# Para execução local, obtenha um Personal Access Token com permissão de
# leitura e escrita em issues/pull requests.
# -----------------------------------------------------------------------------
GITHUB_TOKEN=ghp_SUBSTITUA_PELO_SEU_TOKEN
GITHUB_REPOSITORY=owner/repo

# -----------------------------------------------------------------------------
# Provedor de IA
# Define qual SDK será usado pelos agentes.
# Valores aceitos: openai | anthropic | copilot
# Se não definido, o padrão é openai.
# -----------------------------------------------------------------------------
AI_PROVIDER=openai

# -----------------------------------------------------------------------------
# OpenAI (opcional — obrigatório se AI_PROVIDER=openai)
# OPENAI_API_KEY e AI_PROVIDER_API_KEY são equivalentes; defina apenas um.
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-SUBSTITUA_PELA_SUA_CHAVE
# AI_PROVIDER_API_KEY=ALTERNATIVA_AO_OPENAI_API_KEY

# Modelo a ser usado. Default: gpt-4o-mini
AI_PROVIDER_MODEL=gpt-4o-mini

# URL base do endpoint Chat Completions.
# Default: https://api.openai.com/v1/chat/completions
# Altere apenas se usar um proxy ou Azure OpenAI.
# AI_PROVIDER_URL=https://api.openai.com/v1/chat/completions

# -----------------------------------------------------------------------------
# Anthropic (opcional — obrigatório se AI_PROVIDER=anthropic)
# ANTHROPIC_API_KEY mantém retrocompatibilidade com versões anteriores.
# -----------------------------------------------------------------------------
# ANTHROPIC_API_KEY=sk-ant-SUBSTITUA_PELA_SUA_CHAVE

# -----------------------------------------------------------------------------
# GitHub Copilot SDK (opcional — obrigatório se AI_PROVIDER=copilot)
# Utiliza o mesmo GITHUB_TOKEN definido acima para autenticação.
# Nenhuma chave adicional necessária.
# -----------------------------------------------------------------------------
